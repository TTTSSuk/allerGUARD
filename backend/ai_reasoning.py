import subprocess
import json
import re

def analyze_with_ai(normalized_ingredients, user_allergies):
    """วิเคราะห์สารที่แพ้ด้วย AI"""
    
    prompt = f"""คุณเป็นผู้เชี่ยวชาญด้านส่วนผสมเครื่องสำอาง

📋 รายชื่อส่วนผสมในผลิตภัณฑ์:
{", ".join(normalized_ingredients)}

⚠️ สิ่งที่ผู้ใช้แพ้:
{", ".join(user_allergies)}

งานของคุณ:
1. ตรวจสอบว่ามีส่วนผสมใดที่เกี่ยวข้องกับสิ่งที่ผู้ใช้แพ้หรือไม่
2. ระบุชื่อสารที่พบชัดเจน
3. อธิบายว่าสารนั้นเกี่ยวข้องกับสิ่งที่แพ้อย่างไร
4. ประเมินระดับความเสี่ยง (Low/Medium/High)
5. ให้คำแนะนำว่าควรใช้หรือไม่ใช้

ตอบในรูปแบบนี้เท่านั้น:

พบสารที่เกี่ยวข้อง:

Name: [ชื่อสารที่พบ]
Reason: [เหตุผลว่าทำไมถึงเกี่ยวข้องกับสิ่งที่แพ้]
Risk: [Low/Medium/High]

Name: [ชื่อสารอื่นถ้ามี]
Reason: [เหตุผล]
Risk: [Low/Medium/High]

คำแนะนำ:
[บอกชัดเจนว่าควรใช้หรือไม่ใช้ผลิตภัณฑ์นี้ พร้อมเหตุผล]

ถ้าไม่พบสารที่เกี่ยวข้อง ให้ตอบว่า:

ไม่พบสารที่เกี่ยวข้องกับสิ่งที่คุณแพ้

คำแนะนำ:
ผลิตภัณฑ์นี้น่าจะปลอดภัยสำหรับคุณ แต่ควรทดสอบบนผิวหนังก่อนใช้
"""

    print("⏳ กำลังรอ AI ตอบ (อาจใช้เวลา 30-90 วินาที)...")
    
    try:
        result = subprocess.run(
            [
                "ollama",
                "run",
                "scb10x/llama3.1-typhoon2-8b-instruct"
            ],
            input=prompt,
            text=True,
            capture_output=True,
            encoding="utf-8",
            timeout=1800  # 30 นาที
        )
        
        print("✅ AI ตอบกลับแล้ว!")
        return result.stdout
        
    except subprocess.TimeoutExpired:
        print("❌ AI ตอบช้าเกินไป (timeout)")
        return """
Name: ไม่สามารถวิเคราะห์ได้
Reason: AI ใช้เวลานานเกินไป
Risk: Medium

คำแนะนำ:
กรุณาลองใหม่อีกครั้ง หรือปรึกษาแพทย์
"""
    except Exception as e:
        print(f"❌ เกิดข้อผิดพลาด: {e}")
        return f"""
Name: Error
Reason: {str(e)}
Risk: Unknown

คำแนะนำ:
เกิดข้อผิดพลาดในการวิเคราะห์ กรุณาลองใหม่
"""


def process_with_ai(normalized_ingredients, user_allergies):
    """ฟังก์ชันหลักที่ pipeline เรียกใช้"""
    
    if not user_allergies:
        return {
            "status": "success",
            "detected_allergens": [],
            "message": "ไม่ได้ระบุสิ่งที่แพ้",
            "raw_ai_output": ""
        }
    
    print("🤖 กำลังวิเคราะห์ด้วย AI...")
    ai_output = analyze_with_ai(normalized_ingredients, user_allergies)
    
    print("\n===== AI Response =====")
    print(ai_output)
    print("=======================\n")
    
    # Parse ผลลัพธ์
    detected = []
    
    # หา pattern "Name: xxx"
    name_matches = re.findall(r'Name:\s*(.+)', ai_output, re.IGNORECASE)
    reason_matches = re.findall(r'Reason:\s*(.+)', ai_output, re.IGNORECASE)
    risk_matches = re.findall(r'Risk:\s*(\w+)', ai_output, re.IGNORECASE)
    
    for i, name in enumerate(name_matches):
        # ข้ามถ้าเป็น error หรือ ไม่สามารถวิเคราะห์
        if "error" in name.lower() or "ไม่สามารถ" in name:
            continue
            
        detected.append({
            "ingredient": name.strip(),
            "reason": reason_matches[i].strip() if i < len(reason_matches) else "ไม่ระบุ",
            "level": risk_matches[i].strip() if i < len(risk_matches) else "Medium"
        })
    
    return {
        "status": "success",
        "detected_allergens": detected,
        "raw_ai_output": ai_output
    }